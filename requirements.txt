# Sistema de Inventario Visual — ISO 27001
# Versiones fijas para reproducibilidad y gestión de CVEs (Control A.8.8)
# Actualizar con: pip install --upgrade -r requirements.txt
# Auditar CVEs con: pip audit

# Detección de objetos
ultralytics>=8.0.0,<9.0.0

# Criptografía — NUNCA usar versiones < 41.0.0 (CVEs críticos anteriores)
cryptography>=42.0.0,<45.0.0

# Procesamiento de imágenes
Pillow>=10.3.0,<12.0.0

# Procesamiento de video (opcional pero recomendado)
opencv-python>=4.9.0,<5.0.0

# Auditoría de dependencias (recomendado para CI/CD)
# pip-audit>=2.7.0
